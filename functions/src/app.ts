// EXPRESS ////////////////////////////////////////////////
const express = require('express');
const app = express;

module.exports = app;

/*
app.get ('/hello', (req: any, res: any) => {
  cors( req, res, () => {
    res.send("Hello from a Severless Database!");
  });
  }
)*/
//////////////////////////////////////////////////////////
